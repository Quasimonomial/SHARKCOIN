// TODO: Write an ERC 20 token
// TODO: Add loan functionality
// TODO: Add "burning coins for reputation" functionality
// TODO: Check destination of transaction and that it is a user with a user thing
// TODO: Allow rules to be set for these loans

pragma solidity ^0.4.8;

contract SharkCoin {
  string public constant name = "Shark Coin";

  string public constant symbol = "SHK";

  uint8 public constant decimals = 0;
  /* function totalSupply() public constant returns (uint);

  function balanceOf(address tokenOwner) public constant returns (uint balance);

  function transfer(address to, uint tokens) public returns (bool success);

  event Transfer(address indexed from, address indexed to, uint tokens); */
}
